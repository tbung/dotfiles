- defaults:
  link:
    relink: true

- clean: ['~']

- link:
  ~/.dotfiles: ''
  ~/.tmux.conf: tmux.conf
  ~/.tmuxp: tmuxp
  ~/.vim: vim
  ~/.vimrc: vimrc
  ~/.zshrc: zshrc
  ~/.tldrrc: tldrrc
  ~/.mume: mume


- shell:
  - [git submodule update --init --recursive, Installing submodules]
